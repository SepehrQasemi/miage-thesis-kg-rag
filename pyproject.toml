[project]
name = "miage-thesis-kg-rag"
version = "0.1.0"
description = "Extraction, knowledge graph, and RAG pipeline for MIAGE thesis PDFs."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
